name: First Workflow
on: push
jobs:                        //Jobs will run parallel until we specify dependency
  sample:
    runs-on: ubuntu-latest
    steps:                   //Steps will run sequentially
      - name: sample step
        run: echo "Hello From GitHub-Actions"

  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Testing job
        run: echo "Testing Job From GitHub-Actions"
